# thessla-green-modbus

Asynchronous, transport-independent Python device library for Thessla Green AirPack ventilation units.

The application supplies a `modbus_connection.ModbusUnit`; the device library owns the register map, typed component models, and validated device operations, not the transport.

The initial implementation is being developed on the `develop` branch. No package has been published yet.
